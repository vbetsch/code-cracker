{
  "extends": ["./tsconfig.json", "@vbetsch/config-tsconfig/build/base"],
  "compilerOptions": {
    /* IMPORTANT: You have to define it ! */
    "outDir": "./dist/"

    /* Overrides */
  },
  /* Overrides */
  "exclude": ["**/tests/**"]
}
